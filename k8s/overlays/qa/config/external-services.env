# QA 外部自建 PostgreSQL（独立域名）；部署前必须改为实际值
# 需先建好 lomva / lomva_plugin 两个库，并在 lomva 库内 CREATE EXTENSION IF NOT EXISTS "uuid-ossp"
DB_HOST=qa-postgres-default.xsl.link
DB_PORT=5432
DB_USERNAME=dev_svc_lomva