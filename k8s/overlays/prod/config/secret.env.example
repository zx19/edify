# 线上环境机密覆盖（behavior: merge 合并进 base 的 lomva-secret，只需写不同的 key）
# 外部 TencentDB PG 密码（必须填写）；其余密钥务必同步更换（见 base/config/lomva-secret.env）
DB_PASSWORD=CHANGE_ME

# 生产建议显式生成（openssl rand -base64 42），确定性更强且便于迁移；
# 留空则由 api 自动生成并持久化到本环境 PVC（注意：生成后不要再改，已加密的模型凭据会失效）
SECRET_KEY=
